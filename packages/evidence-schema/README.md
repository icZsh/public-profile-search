# Evidence schema

The canonical wire contract currently lives in `contracts/openapi.yaml`. This package is
reserved for generated evidence JSON Schema artifacts; application code must not add a
parallel handwritten evidence DTO here.

